# Tamilar World — Admin

React admin panel for editing daily calendar entries.

```bash
cp .env.example .env
npm install
npm run dev
```

Ensure the API is running on http://localhost:4000 and `ADMIN_PASSWORD` is set in `api/.env`.

- URL: http://localhost:5173
- Sign in with the admin password configured on the API server
